import { ArrowUpRight, Check, Radio, ServerCog } from 'lucide-react';
import { obdProviders } from './obdProviders';
import './tracking.css';

export default function ObdResearch() {
  return <div className="obd-research"><div className="obd-research-heading"><div><h3>OBD and telematics support</h3><p>Official integration paths reviewed August 28, 2026. Hardware remains disconnected until its provider authorization is configured.</p></div><span><Radio/>4 researched providers</span></div><div className="obd-provider-list">{obdProviders.map((provider, index)=><article key={provider.name}><div className="obd-provider-rank"><span>{index+1}</span><div><b>{provider.name}</b><small>{provider.hardware}</small></div></div><div><small>Best fit</small><b>{provider.fit}</b></div><div><small>Vehicle coverage</small><span>{provider.coverage}</span></div><div><small>Fleetbase data</small><span>{provider.data}</span></div><div><small>Connection</small><span>{provider.integration}</span></div><div className="obd-provider-status"><span><Check/>{provider.status}</span><a href={provider.url} target="_blank" rel="noreferrer" aria-label={`Open ${provider.name} official documentation`}><ArrowUpRight/></a></div></article>)}</div><div className="obd-research-note"><ServerCog/><p><b>Support recommendation:</b> build Bouncie first for owner-operated rental fleets, add Zubie for fleet accounts, then offer Geotab and Samsara adapters for enterprise customers. Generic Bluetooth code readers are not listed because they do not provide unattended cellular GPS or a secure cloud API.</p></div></div>;
}
